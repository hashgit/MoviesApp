namespace MovieApp.Models
{
    public enum SortFields
    {
        Id,
        Title,
        Classification,
        Genre,
        Rating,
        ReleaseDate
    }

    public enum SortDirection
    {
        Ascending,
        Descending
    }
}